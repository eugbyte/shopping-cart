import { OrderDetailsPipe } from './order-details.pipe';

describe('OrderDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
